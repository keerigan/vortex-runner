name: Android APK Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-24.04
    container:
      image: barichello/godot-ci:4.7.2

    env:
      GODOT_VERSION: '4.7.2'
      APK_PATH: build/android/vortex-runner.apk

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Godot templates
        run: |
          mkdir -p ~/.local/share/godot/export_templates/
          mkdir -p ~/.config/
          if [ -d /root/.config/godot ]; then
            mv /root/.config/godot ~/.config/godot
          fi
          mv "/root/.local/share/godot/export_templates/${GODOT_VERSION}.stable" \
             "$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable"
          godot --version

      - name: Validate Godot project
        run: |
          set +e
          godot --headless --verbose --path . --editor --quit > godot-validation.log 2>&1
          STATUS=$?
          cat godot-validation.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script|Error importing|out of bounds" godot-validation.log; then
            echo "Godot project validation failed"
            exit 1
          fi
          if [ "$STATUS" -ne 0 ]; then
            exit "$STATUS"
          fi

      - name: Capture real visual review
        run: |
          apt-get update
          apt-get install -y xvfb libxcursor1 libfontconfig1 libwayland-cursor0 libxkbcommon0 libxinerama1 libxi6 libxrandr2 libxrender1 libxext6 libgl1-mesa-dri
          rm -f visual-review.png visual-review.log
          set +e
          xvfb-run -a -s "-screen 0 540x960x24" godot --display-driver x11 --rendering-method gl_compatibility --path . --resolution 540x960 res://visual_review.tscn > visual-review.log 2>&1
          STATUS=$?
          cat visual-review.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script|Error importing|out of bounds|Unable to create DisplayServer" visual-review.log; then
            echo "Visual review rejected: Godot reported an error"
            exit 1
          fi
          if [ "$STATUS" -ne 0 ]; then
            exit "$STATUS"
          fi
          test -s visual-review.png

      - name: Upload visual review
        uses: actions/upload-artifact@v4
        with:
          name: visual-review
          path: visual-review.png
          if-no-files-found: error

      - name: Build Android APK
        run: |
          mkdir -p build/android
          set +e
          godot --headless --verbose --path . --export-debug "Android" "${APK_PATH}" > godot-export.log 2>&1
          STATUS=$?
          cat godot-export.log
          if grep -E "SCRIPT ERROR|Parse Error|Failed to load script|Error importing|out of bounds" godot-export.log; then
            echo "APK rejected: Godot reported project errors"
            exit 1
          fi
          if [ "$STATUS" -ne 0 ]; then
            exit "$STATUS"
          fi
          test -f "${APK_PATH}"
          ls -lh "${APK_PATH}"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: vortex-runner-apk
          path: ${{ env.APK_PATH }}
          if-no-files-found: error

      - name: Publish latest development release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v3
        with:
          tag_name: build-${{ github.run_number }}
          name: Vortex Runner - Latest
          body: Dernier APK construit automatiquement depuis la branche main.
          files: ${{ env.APK_PATH }}
          fail_on_unmatched_files: true
          overwrite_files: true
          make_latest: true

      - name: Publish versioned release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: ${{ env.APK_PATH }}
          fail_on_unmatched_files: true
          overwrite_files: true
          generate_release_notes: true
          make_latest: true
